[workspace]

[package]
name = "threatflux-hashing"
version = "1.7.0"
edition = "2021"
rust-version = "1.95.0"
authors = ["ThreatFlux"]
description = "High-performance async file hashing library supporting MD5, SHA256, SHA512, and BLAKE3"
license = "MIT"
repository = "https://github.com/ThreatFlux/threatflux-hashing"
keywords = ["hash", "crypto", "async", "hashing"]
categories = ["cryptography", "filesystem", "asynchronous"]
readme = "README.md"

[dependencies]
blake3 = "1.8.6"
md-5 = "0.11"
sha2 = "0.11"
tokio = { version = "1.53.1", features = ["rt", "macros", "fs", "sync"] }
serde = { version = "1.0.229", features = ["derive"], optional = true }
thiserror = "2.0.20"
futures = "0.3.33"

[dev-dependencies]
tempfile = "3.27.0"
tokio = { version = "1.53.1", features = ["full"] }
criterion = { version = "0.8.2", features = ["html_reports"] }
hex = "0.4.3"
serde_json = "1.0.151"
pretty_assertions = "1.4.1"

[features]
default = ["serde"]
serde = ["dep:serde"]

[[bench]]
name = "hash_benchmarks"
harness = false
